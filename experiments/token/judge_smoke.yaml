name: emtok-judge-smoke
setup: bash experiments/token/setup_judge.sh
command: bash experiments/token/judge_smoke.sh
gpus: 1
use_shared: true
env:
  PYTHONUNBUFFERED: "1"
  HF_HOME: /home/brendan/.cache/huggingface
  VLLM_USE_V1: "1"
secrets: [HF_TOKEN]
max_runtime_min: 120
estimated_runtime_min: 45
priority: 20
